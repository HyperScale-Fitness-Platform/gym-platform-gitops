apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  # No eks.amazonaws.com/role-arn annotation: S3 access for presigned uploads is
  # granted via an EKS Pod Identity association (aws_eks_pod_identity_association
  # "gateway_s3" in gym-platform-terraform), which keeps the AWS account ID out
  # of this manifest.
