apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-service
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: operations-service
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }