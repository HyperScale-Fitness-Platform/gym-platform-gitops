apiVersion: v1
kind: Service
metadata:
  name: gym-order-service
  labels:
    app: gym-order-service
spec:
  type: ClusterIP
  selector:
    app: gym-order-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
