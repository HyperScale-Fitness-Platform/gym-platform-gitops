apiVersion: apps/v1
kind: Deployment
metadata:
  name: gym-profile-service
  labels:
    app: gym-profile-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gym-profile-service
  template:
    metadata:
      labels:
        app: gym-profile-service
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.35.0
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "until nc -z profile-postgres 5432; do echo waiting for profile-postgres; sleep 2; done;"]
        - name: wait-for-kafka
          image: busybox:1.35.0
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "until nc -z kafka 9092; do echo waiting for kafka; sleep 2; done;"]
      containers:
        - name: app
          # Placeholder image; the overlay kustomize images transformer +
          # argocd-image-updater rewrite this to the ECR repo digest/tag.
          image: gym-profile-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4002
              name: http
          envFrom:
            - configMapRef:
                name: profile-service-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: profile-svc-credentials
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
